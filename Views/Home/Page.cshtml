@model PageData

<div class="col-2 StaticPageImage @Model.Category @Model.Key"></div>
<div class="h2 col-2">@Model.Name</div>
@foreach (string para in @Model.Summary)
{
    <div class="spacedline col-2">@para</div>
}